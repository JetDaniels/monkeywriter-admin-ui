<!DOCTYPE HTML>
<html>
<head>
    <title>Admin-UI</title>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
    <link href="/css/bootstrap.css" rel="stylesheet">
    <link href="/css/bootstrap-grid.css" rel="stylesheet">
    <link href="css/custom.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <span class="navbar-brand h1"></span>
        <div class="collapse navbar-collapse">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link active" href="/home">Запросы</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/users">Пользователи</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/connections">Соединения</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/documents">Документы</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/roles">Роли</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/systems">Системы</a>
                </li>
            </ul>
            <form class="form-check-inline" action="/logout" method="POST">
                <button type="submit" class="btn btn-primary">Выйти</button>
            </form>
        </div>
    </nav>
    <div class="container info_block mb-3">
        <div>
        <div class="btn-group" role="group">
            <form class="form-check-inline" action="/send" method="GET">
                <button type="submit" class="btn btn-primary btn-menu-inline">Создать запрос</button>
            </form>
        </div>
    </div>
    </div>
    <div class="container info_block mb-3">
        <table class="table table-striped">
        <thead>
            <tr>
                <th>Запрос</th>
                <th>Система</th>
                <th>Соединение</th>
                <th>Кол-во. док-ов.</th>
                <th>Время запроса</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {{#req}}
            <tr>
                <td>{{requestId}}</td>
                <td>{{outsideSystem}}</td>
                <td>{{connection}}</td>
                <td>{{docsCount}}</td>
                <td>{{timeBegin}}</td>
                <td>
                    <form class="form-check-inline" action="/request" method="GET">
                        <input type="hidden" name="_id" value="{{id}}"/>
                        <button type="submit" class="btn btn-info">Инфо</button>
                    </form>
                </td>
            </tr>
        {{/req}}
        </tbody>
    </table>
    </div>
</body>
</html>